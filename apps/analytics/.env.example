# Shared site password (required). Anyone who knows this can view Analytics.
SITE_PASSWORD=change-me

# Random secret used to sign the auth cookie (required in production).
# Generate with: openssl rand -hex 32
AUTH_SECRET=replace-with-a-long-random-string
